ignore-scripts=true
min-release-age=7
# @privy-io/node ships fast and the Privy stack needs a current one; exempt just
# that package from the age guard, which still applies to everything else.
min-release-age-exclude[]=@privy-io/node
save-exact=true
audit=true
fund=false